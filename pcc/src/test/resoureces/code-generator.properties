# tables
tables=pcc_chat

# author
author=yuanli

module_name=pcc

# Java Path
java_path=src/main/java

resources_path=src/main/resources

base_package=group.cc.pcc

# base_package + . + model_package
model_package=model

# base_package + . + mapper_package
mapper_package=dao

# base_package + . + service_package
service_package=service

# base_package + . + service_imp_package
service_impl_package=service.impl

# base_package + . + controller_package
controller_package=controller

# mapper_implements_interface
mapper_implements_interface=group.cc.core.Mapper

# jdbc
jdbc_url=jdbc:mysql://106.12.215.87:3306/cc?useSSL=false&useUnicode=true&characterEncoding=UTF-8

jdbc_username=root

jdbc_password=rootroot

# jdbc driver class
jdbc_driver_class=com.mysql.jdbc.Driver